HERO ARTWORK
============
Optional. The hero section currently uses a pure CSS gradient + grain
background, no image required.

To use a custom hero image/still instead:
1. Add your file here (WebP recommended for performance, 1920px wide max).
2. Set `hero.artwork` in /config/site.config.js to the filename.
3. main.js will apply it as the hero background automatically.
